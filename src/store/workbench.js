/* workbench store */
import Vue from 'vue'

const state = {}
const getters = {}
const mutations = {}

export default {
  state,
  getters,
  mutations
}

export const dataTransfer = Vue.observable({
  data: {}
})
